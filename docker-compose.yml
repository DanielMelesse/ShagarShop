services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: sheger
      POSTGRES_PASSWORD: sheger
      POSTGRES_DB: shegershop
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U sheger -d shegershop"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
